// 振り返り表示（前日の弱点傾向・本日の重点）。Step F で実装。
export default function Review() {
  return (
    <div className="page">
      <div className="card">
        <h2>振り返り</h2>
        <p className="muted">準備中（後のステップで実装します）。</p>
      </div>
    </div>
  )
}
